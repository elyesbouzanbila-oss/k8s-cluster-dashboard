apiVersion: v1
kind: Secret
metadata:
  name: dashboard-redis-password
type: Opaque
stringData:
  # Generate a strong password:
  #   openssl rand -base64 24
  # Then replace the value below:
  redis-password: "change-me-to-a-real-password"
