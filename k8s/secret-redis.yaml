apiVersion: v1
kind: Secret
metadata:
  name: dashboard-redis-password
type: Opaque
stringData:
  # IMPORTANT: Replace with a real secret before deploying!
  # Generate one: openssl rand -base64 32
  redis-password: "change-me-to-a-real-secret"
