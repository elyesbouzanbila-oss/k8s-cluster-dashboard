apiVersion: v1
kind: Secret
metadata:
  name: dashboard-api-key
type: Opaque
stringData:
  # IMPORTANT: This file is .gitignored (see project .gitignore).
  # Before deploying to a cluster, generate the real secret there:
  #   API_KEY=$(openssl rand -base64 32)
  #   kubectl create secret generic dashboard-api-key -n k8s-dashboard \
  #     --from-literal=api-key="$API_KEY" --dry-run=client -o yaml \
  #   | kubectl apply -f -
  api-key: "change-me-to-a-real-secret"
