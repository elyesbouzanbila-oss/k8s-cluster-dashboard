apiVersion: v1
kind: Secret
metadata:
  name: dashboard-api-key
type: Opaque
stringData:
  # ❗ Generate a strong key:
  #   openssl rand -base64 32
  # Then replace the value below:
  api-key: "change-me-to-a-real-key"
