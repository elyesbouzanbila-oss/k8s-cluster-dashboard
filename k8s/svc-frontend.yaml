apiVersion: v1
kind: Service
metadata:
  name: dashboard-frontend
  labels:
    app: dashboard-frontend
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5173
      protocol: TCP
  selector:
    app: dashboard-frontend
